data Bool where
  False : Bool
  True : Bool

data Maybe where
    Just : _ -> Maybe
    Nothing : Maybe

stringify : _ -> String
stringify () = "unit"
stringify (x, y) = (stringify x, stringify y)
stringify (Just x) = Just (stringify x)
stringify Nothing = "Nothing"
stringify _ = "value"

main : _
main = stringify (Just 100)
